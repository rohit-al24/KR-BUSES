# Supabase configuration for local development
# Copy this file to `.env` and fill in your values.
# Do NOT commit your real keys.

###############################
# Frontend (Vite) variables   #
###############################
# Used by the React app in the browser. Must be the anon/public key.
VITE_SUPABASE_URL="https://YOUR_PROJECT_ID.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-public-key"

# Optional: If you run a separate backend and want to override the dev proxy
# VITE_API_URL="http://localhost:3000"

# URL of your Supabase project (server-side)
SUPABASE_URL="https://xyzcompany.supabase.co"

# Prefer service role key for a trusted server (gives full DB access)
# WARNING: keep this secret and never expose it to client-side code.
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Alternatively you can use the anon key (limited privileges) for testing (server-side)
SUPABASE_KEY="your-anon-key-here"

# Example users table schema (Postgres). Run this in the SQL editor in Supabase or psql:
#
# CREATE TABLE public.users (
#   id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
#   email text NOT NULL UNIQUE,
#   name text,
#   dob text,
#   role text NOT NULL, -- 'student' or 'staff'
#   paid boolean DEFAULT false,
#   created_at timestamptz DEFAULT now()
# );
#
# Note: This app uses the DOB as the password for prototype/demo. In production, store hashed passwords and use proper auth.
# After creating the table, you can insert a test user like:
# INSERT INTO public.users (email, name, dob, role, paid) VALUES ('carol.lee@gmail.com','Dr. Carol Lee','1980-07-20','staff', true);

###############################
# Usage notes                  #
###############################
# - For the React app, ensure VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are set in a `.env` file at the project root.
# - For any Node server code, set SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY (or SUPABASE_KEY) in the environment.
# - Keep service role keys secret. Never expose them to the client/browser.
